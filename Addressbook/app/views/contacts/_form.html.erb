<%= simple_form_for([@addbook, @addbook.contacts.build]) do |f| %>

	<%= f.input :name, label: "Name" %>
	<%= f.input :address, label: "Address" %>
	<%= f.input :email, label: "Email" %>
	<%= f.input :phone, label: "Phone Number" %>
	<%= f.button :submit %>


	<% end %>